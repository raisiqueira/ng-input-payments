import { CardExpireDateDirective } from './card-expire-date.directive';

describe('CardExpireDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CardExpireDateDirective();
    expect(directive).toBeTruthy();
  });
});
